const API_BASE_URL = '/api/v1';

const CATEGORY_URL = `${API_BASE_URL}/category`;

const SUBCATEGORY_URL = `${API_BASE_URL}/subcategory`;

const SERVICE_URL = `${API_BASE_URL}/service`;

module.exports = {
    API_BASE_URL,
    CATEGORY_URL,
    SUBCATEGORY_URL,
    SERVICE_URL,
};
